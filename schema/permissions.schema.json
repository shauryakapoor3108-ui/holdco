{
	"$schema": "http://json-schema.org/draft-07/schema#",
	"title": "Permissions file",
	"description": "knowledge/permissions.json — the single-source safety policy. Seeded by KnowledgeStore.ensure() (src/knowledge/store.ts); consumed by KnowledgeStore.policyFor(), which resolves {worktree}/{scopedDir} tokens per card, and enforced natively by each harness adapter. Strict contract: no additional properties. Version 1.",
	"version": 1,
	"type": "object",
	"required": ["version", "writeScopes", "denyCommands"],
	"additionalProperties": false,
	"properties": {
		"version": { "type": "integer", "minimum": 1 },
		"writeScopes": {
			"type": "array",
			"items": { "type": "string", "minLength": 1 }
		},
		"denyCommands": {
			"type": "array",
			"items": { "type": "string" }
		}
	}
}
